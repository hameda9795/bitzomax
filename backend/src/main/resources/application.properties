spring.application.name=bitzomax

# H2 Database Configuration for Development
spring.datasource.url=jdbc:h2:mem:bitzomaxdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Server Configuration
server.port=8080

# File Upload Properties
spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=2KB
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB

# File Storage Locations
file.upload.songs=./uploads/songs
file.upload.covers=./uploads/covers

# JWT Configuration
jwt.secret=bitzomax_secure_jwt_secret_key_for_admin_panel
jwt.expiration=86400000

# PostgreSQL configuration (commented out for now)
# spring.datasource.url=jdbc:postgresql://localhost:5432/bitzomax
# spring.datasource.username=postgres
# spring.datasource.password=postgres
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
